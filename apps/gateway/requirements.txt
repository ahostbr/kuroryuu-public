# Kuroryuu Gateway Requirements
# Provider-agnostic LLM gateway with AG-UI compliance

# Configuration
python-dotenv>=1.0.0

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# HTTP client (for backends + MCP)
httpx>=0.26.0
aiohttp>=3.9.0  # For threat intelligence lookups

# LLM Backends
anthropic>=0.40.0  # Claude SDK

# Optional: Streaming utilities
sse-starlette>=1.8.0  # If using SSE package instead of StreamingResponse

# Marketing module
ddgs>=7.0.0  # Research engine (web search, replaces duckduckgo-search)
playwright>=1.40.0        # Web scraper (headless browser)
readability-lxml>=0.8.0   # Web scraper (content extraction)
markdownify>=0.11.0       # Web scraper (HTML→Markdown)
